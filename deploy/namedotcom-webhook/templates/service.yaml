apiVersion: v1
kind: Service
metadata:
  name: {{ include "namedotcom-webhook.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "namedotcom-webhook.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "namedotcom-webhook.name" . }}
